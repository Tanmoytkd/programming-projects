#include <bits/stdc++.h>
using namespace std;
#define N 100007
#define blk 317
int n, q;
int arr[N], origin[N];
int lazy[blk];
multiset<int> s[blk];

int main() {
	
	scanf("%d%d", &n, &q);
	rootSize = sqrt(n);
	rootBlocks = (n+rootSize-1)/rootSize;
	
	return 0;
}